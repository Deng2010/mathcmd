#include <stdio.h>
#include <string>
using namespace std;
bool isEqual(char a[], char b[]) {
  while () {
  }
  return 1;
}
inline void solvex() {
  char opt[27];
  while (opt[0] != 'e') {
    printf("mathcmd->solvex> ");
    scanf("%s", opt);
  }
}
